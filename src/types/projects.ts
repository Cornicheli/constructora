export interface Project {
    title: string;
    subTitle: string;
    pill: string;
    filter: string;
    photoCount: number;
    image: string;
}

export interface ProjectFilter {
    key: string;
    label: string;
}
