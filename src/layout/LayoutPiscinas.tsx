import CardPiscinas from '../components/CardPiscinas'

const LayoutPiscinas = () => {
      return (
            <main className=" flex items-center justify-center flex-col py-10 relative">
                  <div className="absolute -top-20" />
                  <CardPiscinas />
            </main>
      );
};

export default LayoutPiscinas;
