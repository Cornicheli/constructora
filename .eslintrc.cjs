module.exports = {
    // Otras configuraciones
    extends: [
        // Otras extensiones
        'plugin:react-refresh/recommended',
    ],
    plugins: [
        // Otros plugins
        'react-refresh',
    ],
    rules: {
        // Puedes desactivar la regla si no es necesaria
        'react-refresh/only-export-components': 'off',
    },
};
